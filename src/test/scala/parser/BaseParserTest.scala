package rclang
package parser

import lexer.Token

trait BaseParserTest extends RcBaseParser {
//  def expectSuccess[T](token: Seq[Token], expect: T) = {
//    apply(token) match {
//      case Left(value) => assert(false, value.msg)
//      case Right(value) => assert(value == expect)
//    }
//  }
}
